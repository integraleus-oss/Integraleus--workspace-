# Alpha Platform Preflight - 2026-06-11

Scope: read-only diagnostics for the deployed Alpha Platform stand on
`openclaw-home`. No deployed Alpha files, configs, services, databases, or
mounts were changed.

## Checklist

- [x] Product guardrail read: `docs/alpha_platform/PRODUCT_CHEATSHEET.md`
- [x] Reports config/status inspected
- [x] Security config/status inspected
- [x] WebViewer config/status inspected
- [x] Active ports and systemd overrides inspected
- [x] Plan with rollback points written
- [x] Approved remediation applied on 2026-06-11

## Remediation Applied - 2026-06-11

User approved applying the preflight plan. Changes made:

- Started existing Docker container `alpha-bpr-postgres`.
- Set `alpha-bpr-postgres` restart policy to `unless-stopped`.
- Installed and enabled `slapd`.
- Added Alpha.Security LDAP schema from
  `/opt/Automiq/Alpha.Security/alpha.security.ldif`.
- Applied Alpha.Security LDAP access rules from
  `/opt/Automiq/Alpha.Security/access.ldif`.
- Updated `/opt/Automiq/Alpha.Security/alpha.security.agent.xml`:
  `LdapPassword` now contains an encrypted value generated by the vendor
  utility `/opt/Automiq/Alpha.Security/Utils/alpha.security.crypter`.
- Restarted `alpha.security.service`.
- Installed and enabled `nginx`.
- Added nginx site `/etc/nginx/sites-available/alpha-hmi-demo` and enabled it.

Rollback points:

- Alpha config and systemd backups:
  `output/alpha-platform-preflight-2026-06-11/backups-20260611-142802/`
- PostgreSQL container rollback:
  `docker update --restart no alpha-bpr-postgres && docker stop alpha-bpr-postgres`
- OpenLDAP rollback:
  `sudo systemctl stop slapd && sudo apt-get purge slapd`
- nginx rollback:
  `sudo systemctl stop nginx && sudo apt-get purge nginx nginx-common`

Post-change verification:

- Alpha.Reports connected to DB `ar_base` on PostgreSQL 16 and listens on
  `*:5000`.
- `curl -I http://127.0.0.1:5000/` returns `200 OK`.
- LDAP bind works against `ldap://127.0.0.1:389` as
  `cn=admin,dc=maxcrc,dc=com`.
- After Security restart, no new LDAP errors were observed in
  `alpha.security.service` logs since `2026-06-11 14:34:30`.
- nginx listens on `*:80`.
- `curl -I http://127.0.0.1/demo/build/index.html` returns `200 OK`.
- `curl -I http://192.168.68.125/demo/build/index.html` returns `200 OK`.
- All 10 Alpha services plus `slapd` and `nginx` are active/running.

Remaining non-blocking observations:

- Alpha.Reports still logs failed checks for optional data connectors:
  local OPC UA `127.0.0.1:62544` and Alpha.Link. The web app itself is up.
- Alpha.Security still has AP audit connection messages for
  `127.0.0.1:4388`; the high-frequency LDAP error loop is gone.
- Alpha.Historian licensing warning remains unchanged from preflight.

## Current Service State

All Alpha services are `active/running`:

- `alpha.accesspoint.service`
- `alpha.domain.service`
- `alpha.historian.server.service`
- `alpha.hmi.webviewer.service`
- `alpha.imitator.service`
- `alpha.net.service`
- `alpha.reports.service`
- `alpha.security.service`
- `alpha.security.useractivity.service`
- `alpha.server.service`

System-wide failed units are unrelated Synology NFS mounts:

- `mnt-synology-Documents.mount`
- `mnt-synology-homes.mount`
- `mnt-synology-music.mount`
- `mnt-synology-video.mount`

## Active Ports

Alpha-relevant listeners:

- `4572` - `Alpha.Server`
- `4600` - `alpha.historian`
- `4949` - `alpha.historian`
- `4976` - `Alpha.AccessPoint`
- `4983` - `Alpha.Imitator`
- `8080` - `Alpha.HMI.WebViewer` WebSocket runtime
- `11010` - `alpha.net.agent`
- `11020` - `alpha.net.agent`

Missing expected listeners:

- `5000` - configured for Alpha.Reports Kestrel, but not listening
- `80` - no web server serving `/var/www/html/demo`
- `54329` - Alpha.Reports logs show DB attempts to this port, but no listener

No loaded `postgresql*`, `nginx*`, `apache2*`, or `caddy*` systemd services
were found.

## Alpha.Reports

Service:

- Unit: `/etc/systemd/system/alpha.reports.service`
- Runs as `root`
- WorkingDirectory: `/opt/Automiq/Alpha.Reports`
- ExecStart: `/opt/Automiq/Alpha.Reports/Alpha.Reports`
- Restart: `always`

Config summary from `/opt/Automiq/Alpha.Reports/appsettings.json`:

- Kestrel URL: `http://*:5000`
- `ServerAddress`: `http://127.0.0.1:5000`
- `RedirectToHTTPS`: `false`
- `LdapServer`: empty
- `DataBaseServer`: encrypted/string value present, length 256
- LDAP security servers: `127.0.0.1:389`, `192.168.0.1:389`
- `EntryPoints`: `127.0.0.1:5000`, `192.168.0.1:5000`, `somehostname:5000`
- SMTP still has example values

Observed behavior:

- Process is running, but `127.0.0.1:5000` refuses connections.
- Logs every 15 seconds: DB connection refused to `127.0.0.1:54329`.
- `/opt/Automiq/Alpha.Reports/SQL/dump.sql` and patch SQL files are present.
- A previous backup exists:
  `/opt/Automiq/Alpha.Reports/appsettings.json.bak-openclaw-20260604-1659`.
  It appears to contain a structured DB config with host/port/login/password.

Preflight conclusion:

Alpha.Reports is started but not usable. It appears blocked before binding
Kestrel on `5000` because it cannot connect to its configured PostgreSQL
endpoint.

## Alpha.Security

Service:

- Unit: `/usr/lib/systemd/system/alpha.security.service`
- Runs as `root`
- WorkingDirectory: `/opt/Automiq/Alpha.Security`
- Environment: `LD_LIBRARY_PATH=/opt/Automiq/Alpha.Security`

Config summary from `/opt/Automiq/Alpha.Security/alpha.security.agent.xml`:

- Entry point Net Agent: `127.0.0.1:11010`
- LDAP server: `127.0.0.1:389`
- LDAP user: `cn=admin,dc=maxcrc,dc=com`
- AP/OPC DA server: `127.0.0.1:4388`
- Additional sample sources exist in comments/config: `192.168.1.2:389`,
  `192.168.1.3:389`

Observed behavior:

- Logs every second: wrong LDAP connection user.
- `alpha.security.service` produced about 3647 log lines in one hour.
- Journals currently occupy about 2.5 GiB.

Preflight conclusion:

Alpha.Security is running, but LDAP integration is misconfigured for this lab
environment. This is the largest log-noise source.

## Alpha.Security UserActivity

Service:

- Unit: `/usr/lib/systemd/system/alpha.security.useractivity.service`
- Runs wrapper as `root`
- Script: `/opt/Automiq/Alpha.Security/alpha.security.useractivity.sh`
- Script runs binary as `stanislav`

Script summary:

- `DISPLAY` is set.
- `XAUTHORITY` is set.
- Executes:
  `sudo -u stanislav /opt/Automiq/Alpha.Security/alpha.security.useractivity`

Observed behavior:

- Service is now active.
- Earlier log indicated idle-time retrieval `Connection refused`; no new
  significant entries in the last hour during this preflight.

Preflight conclusion:

Not the main blocker now. Keep as-is unless UI/security testing shows user
activity detection is needed.

## Alpha.HMI.WebViewer

Service:

- Unit: `/usr/lib/systemd/system/alpha.hmi.webviewer.service`
- Override: `/etc/systemd/system/alpha.hmi.webviewer.service.d/override.conf`
- Runs as `root`
- WorkingDirectory: `/opt/Automiq/Alpha.HMI.WebViewer`
- ExecStart after override: `/usr/local/bin/alpha.hmi.webviewer`

Config summary from
`/opt/Automiq/Alpha.HMI.WebViewer/alpha.hmi.webviewer.xml`:

- WebSocket address: `192.168.68.125`
- WebSocket port: `8080`
- ProjectPath: `/opt/alpha-hmi-lab/projects/AlphaHmiRuntimeCheck.hmi`
- WwwRoot: `/var/www/html/demo`

Generated frontend:

- `/var/www/html/demo/build/index.html`
- `/var/www/html/demo/build/script/xwv.js`
- `/var/www/html/demo/build/script/xwv-conf.js`

`xwv-conf.js` points to WebSocket host `192.168.68.125`, port `8080`.

Observed behavior:

- Plain HTTP request to `8080` returns `400` because it is a WebSocket runtime,
  not static file hosting.
- Proper WebSocket handshake to `192.168.68.125:8080` returns
  `101 Switching Protocols`.
- Port `80` refuses connections; no static web server currently serves
  `/var/www/html/demo`.

Preflight conclusion:

WebViewer runtime is alive, but the browser-facing static site is not being
served over HTTP.

## Immutable Reference Points

Current config mtimes/checksums captured for rollback planning:

- `/opt/Automiq/Alpha.Reports/appsettings.json`
  - mtime `2026-06-04 16:59:33 +0300`
  - sha256 `89566bc3e366b8f7db2d143eb951866c113e1f6203ffd5be86c4c464e71d5663`
- `/opt/Automiq/Alpha.Security/alpha.security.agent.xml`
  - mtime `2026-03-03 14:13:44 +0300`
  - sha256 `a0a99f61c8fb88ee13d4bc2e55ae2d6310559b549e2090f6517e4e8cc142dde9`
- `/opt/Automiq/Alpha.Security/alpha.security.useractivity.sh`
  - mtime `2026-06-03 22:48:19 +0300`
  - sha256 `66f038a3729b4552f754a910ab30190a3731c691064d2abee9a2c05744536792`
- `/opt/Automiq/Alpha.HMI.WebViewer/alpha.hmi.webviewer.xml`
  - mtime `2026-06-03 22:48:19 +0300`
  - sha256 `fe9956f145ae5d4a5594d5b884b0b0d6b3deff0c68068368dd94af6cd6536ce4`
- `/etc/systemd/system/alpha.reports.service`
  - mtime `2026-03-13 11:44:38 +0300`
  - sha256 `35ef2773160844d7b9111268f95913cb81f084979e06e016194fddce7497997c`
- `/etc/systemd/system/alpha.hmi.webviewer.service.d/override.conf`
  - mtime `2026-06-03 22:48:19 +0300`
  - sha256 `1b2c52b4a6a39dd5250490a82744613ba194aef7987de17062982d366054d841`

## Proposed Fix Plan

### Step 1 - Alpha.Reports DB

Goal: make Alpha.Reports bind `5000` and stop DB connection loop.

Pre-change rollback point:

- Copy `/opt/Automiq/Alpha.Reports/appsettings.json` to timestamped backup.
- Capture `systemctl status alpha.reports.service`.
- Capture DB listener state with `ss -ltnp | grep 54329`.

Candidate actions to confirm separately:

1. Decide intended DB target:
   - local PostgreSQL on `127.0.0.1:54329`, or
   - regular local PostgreSQL on another port, or
   - external/container DB.
2. If local Reports DB is intended:
   - install/enable/start PostgreSQL only after confirmation,
   - create/import Reports schema from `SQL/dump.sql`,
   - ensure config points to the real DB.
3. Restart only `alpha.reports.service`.

Verification:

- `ss -ltnp` shows `:5000` by `Alpha.Reports`.
- `curl -I http://127.0.0.1:5000/` returns HTTP response, not connection refused.
- Reports logs stop emitting DB connection refused every 15 seconds.

Rollback:

- Restore previous `appsettings.json`.
- Stop any newly started DB service/container if created.
- Restart `alpha.reports.service`.

### Step 2 - Alpha.Security LDAP

Goal: stop one-error-per-second LDAP loop without breaking local lab access.

Pre-change rollback point:

- Copy `/opt/Automiq/Alpha.Security/alpha.security.agent.xml` to timestamped
  backup.
- Capture current log rate and process state.

Candidate actions to confirm separately:

1. If LDAP is needed:
   - confirm real LDAP host, bind DN, password, base DN, and secure mode,
   - update LDAP section accordingly.
2. If LDAP is not needed for lab:
   - identify supported vendor switch to disable LDAP or use local/default user,
   - avoid guessing XML changes until confirmed by docs/config examples.

Verification:

- `journalctl -u alpha.security.service --since '5 minutes ago'` has no repeated
  LDAP connection-user errors.
- `alpha.security.service` remains active.
- Net agent connectivity remains intact on `11010`.

Rollback:

- Restore previous `alpha.security.agent.xml`.
- Restart `alpha.security.service`.

### Step 3 - WebViewer Static HTTP

Goal: serve the generated HMI web app while leaving WebSocket runtime on `8080`.

Pre-change rollback point:

- Capture current web-server state: no `80` listener and no nginx/apache/caddy
  units.
- Do not alter `/opt/Automiq/Alpha.HMI.WebViewer/alpha.hmi.webviewer.xml` unless
  needed.

Candidate actions to confirm separately:

1. Choose web server: nginx is the lightest option for this host.
2. Serve `/var/www/html/demo` or `/var/www/html/demo/build` on port `80`.
3. Keep WebSocket target as `192.168.68.125:8080`.

Verification:

- `curl -I http://127.0.0.1/demo/build/index.html` or chosen URL returns `200`.
- Browser loads the static page.
- WebSocket handshake to `192.168.68.125:8080` still returns `101`.

Rollback:

- Disable/remove the added web-server site.
- Stop the added web-server if it was introduced only for this test.
- Leave WebViewer service untouched.

## Recommended Order

1. Fix Alpha.Reports DB first, because the service is unusable despite being
   active.
2. Fix Alpha.Security LDAP second, because it creates high log noise and journal
   growth.
3. Add WebViewer static HTTP third, because the runtime itself is already healthy.
