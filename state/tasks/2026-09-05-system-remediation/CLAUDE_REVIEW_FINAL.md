{"duration_api_ms":62498,"stop_reason":"end_turn","session_id":"cb44dd08-58c7-407e-89f1-7c52536ff5bb","total_cost_usd":1.05327125,"usage":{"input_tokens":2,"cache_creation_input_tokens":40839,"cache_read_input_tokens":11685,"output_tokens":4671,"output_tokens_details":{"thinking_tokens":3615},"server_tool_use":{"web_search_requests":0,"web_fetch_requests":0},"service_tier":"standard","cache_creation":{"ephemeral_1h_input_tokens":40839,"ephemeral_5m_input_tokens":0},"inference_geo":"not_available","iterations":[{"input_tokens":2,"output_tokens":4671,"cache_read_input_tokens":11685,"cache_creation_input_tokens":40839,"cache_creation":{"ephemeral_5m_input_tokens":0,"ephemeral_1h_input_tokens":40839},"type":"message"}],"speed":"standard"},"modelUsage":{"claude-fable-5-1":{"inputTokens":2,"outputTokens":4671,"cacheReadInputTokens":11685,"cacheCreationInputTokens":40839,"webSearchRequests":0,"costUSD":1.05327125,"contextWindow":1000000,"maxOutputTokens":64000,"thinkingTokens":3615,"canonicalModel":"claude-fable-5-1","provider":"firstParty","costBasis":"list"}},"permission_denials":[],"terminal_reason":"completed","fast_mode_state":"off","fast_mode_disabled_reason":"sdk_opt_in_required","subagent_stats":{"spawned":0,"requested":{"background":0,"foreground":0,"unset":0},"started_in_background":0,"max_depth":0,"spawned_by_subagents":0,"completed":0,"failed":0,"killed":{"parent":0,"user":0,"system":0},"refused":{"depth_limit":0,"concurrency_limit":0,"budget":0},"by_type":{}},"is_error":false,"num_turns":1,"subtype":"success","api_error_status":null,"result":"**Verdict:** the query change is correct and in the right place. No blocking findings. The remaining items are verification gaps I could not close from the diff alone.\n\n**Correctness of the SQL**\n\n- **NULL handling is right.** `recovery_state NOT IN (...)` evaluates to UNKNOWN for NULL rows and would drop them. The explicit `IS NULL` branch in the OR restores legacy rows. Kysely wraps the `eb.or` in parentheses and ANDs it with the `status = 'failed'` clause, so precedence is correct.\n- **Denylist is the right shape.** Excluding only the two settled states means any unknown or future recovery state still surfaces in health. That errs toward over-reporting, which matches the severity order (silent failure worst).\n- **Owner is correct.** The filter lives in `countFailedDeliveryQueueEntries`, the single query the collector reads, rather than as a post-filter in the collector.\n\n**Findings to act on**\n\n1. **Settled-state literals have no single owner.** The query hardcodes `completed_permanent` and `completed_bounded` as strings in `src/infra/delivery-queue-sqlite.ts`. The tombstone prune path in the same file already knows which recovery states are settled tombstones. If a shared constant or the recovery-state union type defines that set, the query should derive from it so the two cannot drift. If none exists, the inline comment is an acceptable contract reason, but check first.\n\n2. **Sibling read surfaces not covered.** Only the count query changed. If any other operator-facing path enumerates `status = 'failed'` rows as dead letters (queue inspect or replay CLI, doctor, status output, `loadDeliveryQueueEntries` with a failed filter), it will still present settled tombstones as active failures. Either confirm no such consumer exists or state the follow-up in the PR.\n\n3. **Test literal `settlement_pending` is unverified.** Both tests write that value with raw SQL. If the column has no CHECK constraint, a typo or renamed state would still pass while asserting a state production never writes. Prefer importing the typed constant, or confirm the literal matches the current recovery-state union.\n\n4. **Possible stale test name.** The terminal test that previously asserted `count: 5, oldestFailedAt: 1_000` now expects `[]`. Its `it(...)` title is outside the hunk. If it still describes health counting retained tombstones, rename it to say tombstones are excluded from health.\n\n**Coverage assessment**\n\nThe three cases in the invariant are each exercised: legacy NULL rows (both outbound and session in the terminal test), `settlement_pending` (dead-2 and dead-active), and exclusion of settled tombstones alongside an included row in the same group (collector test, count 1 with dead-1 excluded). The terminal test's `UPDATE ... SET recovery_state = NULL` before the assertion is what makes the legacy case real, since retained failures otherwise land as `completed_permanent`. That is fine but worth a one-line comment in the test explaining it simulates pre-recovery-state rows.\n\nThe payload was not marked truncated, and the stat matches the hunks shown, so no residual truncation risk.","ttft_ms":49296,"type":"result","duration_ms":62551,"uuid":"573fb53e-22f0-4825-8e0c-ec6e8eb780be","ttft_stream_ms":1902,"time_to_request_ms":52,"first_content_frame_ms":2677,"queued_turn_count":0}
