# .NET Broker Qualification

Run one isolated qualification cycle. Only `Program.cs` may change. The first implementation must intentionally use cwd and fail the focused gate. The builder may issue exactly one authenticated repair; the repaired implementation must use `ORCHESTRATOR_FIXTURE_ROOT` and fail closed. Final gates and independent review must pass. Do not commit, push, deploy, use the network, or touch Alpha BPR.
