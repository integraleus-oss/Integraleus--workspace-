Qualify the .NET orchestrator on an isolated C# fixture using the real Codex sandbox, Trusted Test Broker, one authenticated builder-repair attempt, deterministic execution paths, strict scope checks, and independent review. The controlled first implementation must fail because it resolves a fixture through the current working directory. The trusted builder must classify the failure as repairable, return bounded untrusted evidence, and the same sealed cycle must repair Program.cs to use ORCHESTRATOR_FIXTURE_ROOT. The final restore, focused, and diff gates must pass, then independent review must accept. Only Program.cs may change. No commit, push, deploy, network access, or Alpha BPR execution is allowed inside the pilot.
