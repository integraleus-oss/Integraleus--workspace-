Perform the required targeted verification using every sealed artifact in the
trusted input directory supplied by the runner. Re-verify each carried prior
finding against the current observed tree and gates. Do not infer closure when
evidence is insufficient. Every `appears_fixed` verification must cite command
output or a digest-bound artifact accepted by the sealed contract. Return only
the exact JSON object, with no prose, Markdown fence, prefix, or suffix.
