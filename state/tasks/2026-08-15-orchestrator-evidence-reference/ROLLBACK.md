# Rollback

Revert the narrow workspace commit produced by this task. This restores the
previous prompt/controller behavior and tests. The detached r5-r8 worktrees and
run roots are evidence only and are not part of source `home-agent-factory`.

No service, deployment, Gateway, cron, or source-project rollback is required.
