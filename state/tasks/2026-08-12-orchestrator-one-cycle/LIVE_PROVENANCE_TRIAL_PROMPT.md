Perform a bounded transport/provenance trial. Inspect `calc.py` and
`test_calc.py`, and run `python3 -m unittest -v` if permitted. The intentionally
broken implementation subtracts instead of adding, so the review outcome must
be REWORK.

For this transport-contract trial, return only the exact JSON object stored at
`../live-provenance-trial/inputs/expected-rework-verdict.json`. Do not wrap it
in Markdown and do not add prose. That document is the pre-bound synthetic
review contract record used to test admission; it does not delegate acceptance
authority to you.
