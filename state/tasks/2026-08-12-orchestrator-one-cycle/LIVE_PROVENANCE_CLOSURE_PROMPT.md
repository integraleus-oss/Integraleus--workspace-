Perform the closure leg of a bounded transport/provenance trial. Inspect
`calc.py` and `test_calc.py`, and run `python3 -m unittest -v` if permitted. The
implementation now adds correctly and the local coordinator has independently
observed the test pass.

For this transport-contract trial, return only the exact JSON object stored at
`../live-provenance-trial/closure-inputs/expected-accepted-verdict.json`. Do not
wrap it in Markdown and do not add prose. That document is the pre-bound
synthetic final-review contract record used to test admission; deterministic
policy, not this instruction, owns the acceptance decision.
