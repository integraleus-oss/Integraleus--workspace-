# Alpha BPR Universal Integration R4

Fresh retry from canonical baseline `b9e6377`. R1–R3 implementation outputs are forbidden inputs. Operator flow remains `PREPARE → RUN`; this preparation must not launch Codex or Claude.

R4 uses the qualified Trusted Test Broker, one bounded implementation repair, deterministic project/fixture/artifact roots, external .NET artifacts, sealed prior-failure evidence, strict allowlist, and independent standard review.
