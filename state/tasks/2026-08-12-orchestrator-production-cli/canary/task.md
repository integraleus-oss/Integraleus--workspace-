Canary task: inspect only calc.py and test_calc.py, run `python3 -m unittest -v`,
and confirm the existing add(2, 3) behavior returns 5. The implementation is
expected to be already correct. Do not modify files when the test passes. Do
not commit, push, install dependencies, or inspect secrets.
