This is a closure review used only if deterministic policy requested one bounded
rework. Inspect only calc.py and test_calc.py; run `python3 -m unittest -v`.
Use the trusted manifest and response shape in the absolute review-2 directory
beside this task packet. Return only contract-valid JSON. Do not decide
acceptance; deterministic policy owns it.
