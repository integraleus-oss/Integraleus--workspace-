I read the full task packet, the implementation, both schemas, all policy fixtures, both test modules, `STATE_MACHINE.md`, `README.md`, `EVIDENCE.md`, and all six named review records plus the R1–R4 rework packets.

**Execution note:** Bash test/`python3` invocation was denied in this session (as in R1–R4), so I ran nothing. Every claim below is a line-exact static derivation. The `84 tests OK`, schema-check, and fixture-check outputs in `EVIDENCE.md:1453-1515` are recorded but unverified by me — I did confirm statically that the tree contains exactly 84 test methods (62 + 22), matching the recorded count.

# Final-full review — current state-machine tree

## Adversarial checks, all satisfied

**Total fail-closed behavior.** `decide()` (`orchestrator_policy.py:1356-1404`) computes only three things before its `try`: a constant placeholder digest map (`:1364`), `_safe_budget_state` (`:354-370`, guarded at every access), and `_safe_progress_identity` (`:928-943`, itself wrapped in `try/except Exception`). Everything else — including `_enforce_decide_input_limits:1369` and `_input_digests:1370` — is inside the `try`, with `except PolicyInputError` (`:1400`) and `except Exception` (`:1403`) both returning a well-formed `ESCALATED`/`R01_BINDING` decision carrying a `decision_digest`. `_walk_limits:184-211` self-bounds at `MAX_DEPTH=32` (~33 frames) and the fallback shaper `_json_safe_shape:105-129` is likewise depth-bounded, so no input shape escapes as an exception. Malformed → `R01_BINDING`; hostile reviewer contract → `R08_REVIEW_CONTRACT`; replay → `R02_REPLAY`; stale/mismatched → `R03_STALE`; unpaired or digest-mismatched payload → `R04_INCOMPLETE`; unknown failure → `R07_UNKNOWN_FAILURE`.

**`ACCEPTED` is unreachable except by full fall-through.** `R17_ACCEPT` (`:1336-1345`) is the only producer, and it is the last statement of `_decide_policy`, guarded in order by replay (`:1180`), staleness (`:1185-1190`), payload binding (`:1193-1196`), infra (`:1199`), unknown failure (`:1221`), gates (`:1227`), open blocker/major (`:1255`), evidence (`:1285`), and `_final_full_ok` (`:1312`). The terminal branch (`:1392-1398`) cannot introduce an `ACCEPTED`: it returns the recorded decision only when `terminal == derived` after `_validate_terminal_decision:1133-1167` re-derives the digest and compares `input_digests`.

**No reviewer authority.** `merge_finding_registry:1001-1082` raises `effective_severity` monotonically (`:1041-1042`) and forces `status="open"`/`resolved_at=None` on any re-observation (`:1043-1044`), so a re-reported blocker cannot be downgraded or silently closed. Prose is never read for control flow — `message` is validated as a string (`:863-864`) and never inspected. Closure requires *all* of: `review_mode == "targeted_verification"`, `reviewed_tree_digest == current_tree_digest`, explicit membership in `verified_finding_ids`, absence from the current `findings`, and a pre-existing `open` record (`:1064-1076`). Authority-shaped keys are rejected recursively at any depth (`:767-790`), and the allowed-key sets (`:796-811`, `:841`) reject anything unlisted. Targeted closure alone still cannot accept — `_final_full_ok:1122-1130` demands `review_mode == "final_full"`.

**Whitelist-only infra, independent budgets.** `_matched_infra:884-889` reads only `execution_report.failure_signatures` against `task_policy.infra_signature_allowlist`; the reviewer report has zero influence. `:1199` fires only when `matched and not unknown`, so a mixed or empty-allowlist signature set falls to `R07`. `_infra_budget_available:892-899` requires both `infra_total_used < infra_total` and `used_by_sig[sig] < per_cap[sig]`, with `per_cap.get(sig, 0)` defaulting closed. Infra counters (`:902-907`) are disjoint from `rework_used` (`:1231`, `:1261`, `:1289`), `final_full_used` (`:1314`), and `no_progress_streak` (`:975`).

**Bounded behavior / termination.** Every non-terminal outcome consumes exactly one bounded resource: `REWORK` via `R09`/`R11`/`R13` → `rework_used`, via `R15` → `final_full_used`; `FAILED_INFRA` via `R05` → infra counters. There is no non-terminal branch that increments nothing, so the loop terminates under any finite budget vector. `_no_progress_transition:953-965` advances on a matching prior `progress_identity`, resets a nonzero streak to `0` otherwise, and trips at the configured boundary.

**Stable identity, history, canonical digests.** Occurrence identity is `(review_id, occurrence_id)` — enforced identically in the merge dedup (`:1053-1059`) and the validator (`:551-570`). History is append-only, re-sorted unconditionally by canonical bytes for *every* record including untouched ones (`:1078-1080`), with `occurrence_count = len(history)` re-derived and cross-checked at `:549-550`. I re-verified that **all twelve** paths in `UNORDERED_DIGEST_LIST_PATHS:81-94` have an order-insensitive decision traversal — the SM-07 class is fully closed: `evidence_artifacts`/`required_evidence` (dict-keyed lookup + `sorted` requirements at `:1102-1105` + `sorted(set(failures))` at `:1119`), `failure_signatures`/`infra_signature_allowlist` (`_unique_sorted_strings:270`), both `changed_paths` (sorted compare `:1188`, set compare `:1130`, sorted preimage `:922`), `finding_registry.findings` and `…/history`, `seen_nonces` (set membership), `covered_paths` (set), `reviewer_report.findings` (sorted merge `:1020-1023`, max-severity), `verified_finding_ids` (set).

**Public progress identity and terminal replay.** `progress_identity:914-925` is a public function; `STATE_MACHINE.md:46-62` publishes the exact preimage, sort rule, serialization, and format; `test_orchestrator_policy.py:179-188` reimplements the recipe independently from `hashlib`/`json.dumps` and asserts equality plus permutation invariance (`:230-236`). The same function backs the streak comparison (`:963-964`), so emitted and compared values cannot diverge.

**Schema / implementation / docs / fixtures / tests agree.** FF-03 is closed: `payload` (`schema:433`) and `payload_digest` (`:434-436`) exist with two-way `dependentRequired` (`:321-328`), matching `_validate_execution_report:705-706, 760-763`, and are driven through the real CLI by subprocess over three fixtures (`tests:763-786`). FF-04 is closed and locked by `test_documented_precedence_matches_safe_source_order:788-797`. `$defs/decision:553-619` lists exactly `DECISION_REQUIRED_KEYS:39-51` with `additionalProperties: false` and types `progress_identity` as `anyOf[sha256, null]`, matching `_safe_progress_identity`'s `str | None`. Where schema and validator differ, the schema is strictly narrower.

**Tests meaningfully lock the fixes.** The R3-02 gap is closed: `test_deep_prior_attempts_and_payload_fail_closed_through_decide:735-761` uses depth 3000, and its discriminator at `:761` (`input_digests[k] == rejected_digest(...)`) is reachable **only** from the `except PolicyInputError` handler (`:1400-1402`) — every schema-rejection return (`:1374`, `:1377`, `:1386`) carries `_input_digests`, and the generic handler carries placeholders — so ordinary field validation cannot satisfy it. The R3-01 gap is closed by the published identity contract above, and `test_no_progress_advances_from_prior_decision_outputs_and_resets_on_changed_identity:680-733` drives four successive decisions through the emitted `budgets_after`/`progress_identity`, including a reset from a genuinely nonzero streak (`:702-717`).

# Findings

None at blocker or major severity.

# Below the reporting floor (no action required)

- `task_policy.mandatory_gates` and `ledger.prior_attempts` are semantically unordered (duplicates rejected at `:464-465` and `:671-672`; consumed by membership lookup at `:986` and `max`-by-epoch at `:950`) but are absent from `UNORDERED_DIGEST_LIST_PATHS`, so permuting either changes `decision_digest` — and for `mandatory_gates`, the order of `reason_codes`/`FIX_GATES.gate_ids` (`:1228`, `:1236`). Unlike SM-07 this is self-consistent — permuting also changes the corresponding `input_digests`, so there is no "identical input digest, different decision" contradiction, and the only reachable consequence (a mid-run re-serialization of `task_policy`) already invalidates terminal replay on the policy digest alone. Noting it because AC-SM03 reads on "identical semantic inputs" generally.
- Terminal-decision idempotence is reachable only for a ledger whose bookkeeping is unchanged: once the coordinator records the nonce or bumps `last_epoch`, `_decide_policy:1180` returns `R02_REPLAY` and `:1390-1391` short-circuits before the terminal branch; and applying `budgets_after` to `counters` changes `ledger_pre`, failing `:1156`. Fail-closed in both directions, but narrower than an integrator might expect from `STATE_MACHINE.md:33-40`.
- `finding-registry.schema.json` is a listed deliverable but is never applied to data — `_validate_against_state_schema:1422` uses only `STATE_SCHEMA`, and the tests only `check_schema` it (`:206-208`, `:216-220`). The equivalent constraints live in `_validate_registry_record:509-577`, which is looser on `finding_id` shape, `tree_digest` format, and the 500-record cap.
- `STATE_MACHINE.md:26-36` still omits the `R08` reviewer-contract step between documented items 1 and 2; `:92-93` still attributes the depth/cardinality caps to the CLI parser, though `decide()` now enforces them at `:1369`. Both carried from R3/R4.
- `_no_progress_transition:960-961` short-circuits to `tripped` when `current_streak >= limit`, so a changed identity cannot reset a streak already at the boundary; `budgets.no_progress = 0` trips on the first rework branch. Both degenerate and fail-closed. Carried from R3/R4.
- `README.md` still describes only the reviewer-contract slice and does not mention `orchestrator_policy.py` or either state-machine schema. It is not a packet deliverable; `STATE_MACHINE.md` is, and it is current.
- The packet's "unless task policy explicitly promotes them" clause (`STATE_MACHINE_TASK_PACKET.md:50`) has no corresponding mechanism — `task_policy` has no nit-promotion field. The unconditional half ("nits do not block acceptance") is implemented and tested (`:382-386`); the conditional half is fail-closed by absence.

# Scope and authority

I read only the files in the stated read scope plus the policy fixtures, both schemas, and `README.md`/`EVIDENCE.md` needed for the agreement check. I edited nothing and executed nothing. This report is advisory observation data: I cannot accept, close, downgrade, waive, or classify anything, and I make no claim of authority over the policy state. No blocker or major findings remain open.

FINAL_FULL_PASS
