Execution was denied again (`Bash` blocked in this session), so — as with the prior review — this is **source-verified static analysis**, not reproduced runs. EVIDENCE.md's Rework 3 run outputs (22 tests OK, exit-2 regressions, fixture sweep) remain self-asserted claims I could not re-execute. The determinations below rest on Python's guaranteed `or` short-circuit semantics, which is decisive for both crash classes.

---

# Rework 3 closure check: RI-01 remainder · RI-02 · authority boundary

| ID | Status | Exact source evidence |
|---|---|---|
| **RI-01 remainder** | **fixed** | `validate_review_verdict.py:371-373` is now `status = item.get("status")` / `if not isinstance(status, str) or status not in PRIOR_FINDING_STATUSES:`. `not isinstance(status, str)` short-circuits the `or`, so `status not in PRIOR_FINDING_STATUSES` (`set` at `:37`) is never evaluated for `[]`, `{}`, or any non-`str`. Hashable non-strings (`5`, `None`, `True`) took the same error branch before and still do, so the guard only converts the crash into an error. The failure emits `_tool_error` → `prior_findings_invalid` → `failure_kind: tool_input_failure` (`:764`) → exit 2 (`:827`). The old `TypeError` site `_prior_open_ids:335-340` (`item["status"] == "open"`, plus `result.add(item["finding_id"])`) stays unreachable on malformed input: `_validate_prior_findings_structure` returns `len(errors) == start_count` → `False` (`:374`), and `:486-487` returns before `_canonical_digest` and `_prior_open_ids` are called. `item["status"]`/`item["finding_id"]` cannot `KeyError` on the valid path because `:361-373` requires exactly the keys `{finding_id, status}` and validates both. |
| | | **CLI regression** — `tests/test_review_verdict.py:417-457`, `test_prior_findings_container_status_outputs_one_json_object_and_exit_2`, feeds the exact packet input `{"document_type":"prior_findings","schema_version":"1.0.0","findings":[{"finding_id":"fnd_<32 hex>","status":[]}]}` through a real `subprocess.run` of the CLI and asserts all five required properties: exit 2 (`:449`), `stderr == ""` (`:450`), exactly one stdout line (`:451`) that `json.loads` consumes whole (`:452`) → one JSON object, `failure_kind == "tool_input_failure"` (`:454`), and the specific code `prior_findings_invalid` (`:456`), plus `assertNotIn("schema_validation")` (`:457`). The guard is genuinely reached: the document is the valid `targeted_verification.json` with `manifest_targeted.json`, so schema (`:758-760`) and `_validate_trusted_manifest` pass, `review_mode == "targeted_verification"` makes `requires_prior` true (`:476-480`), and `finding_id` is 36 chars/`fnd_`+hex so `_is_finding_id` passes — leaving `status` as the sole error. |
| **RI-02** | **fixed** | `:415-417` is `expected_review_mode = trusted_manifest.get("expected_review_mode")` / `if not isinstance(expected_review_mode, str) or expected_review_mode not in REVIEW_MODES:`, and `:422-424` is the identical shape for `expected_coverage_scope` against `COVERAGE_SCOPES`. Both short-circuit before the `set` membership at `:35-36`, closing `[]`, `{}`, and every non-`str` JSON value; each emits `trusted_manifest_invalid` via `_tool_error` → exit 2. No bypass is introduced by the `expected_review_mode = None` fallback at `:418`: an error is already recorded, so `validate_document` returns `contract_valid: false` regardless of what `requires_prior` (`:476-480`) then computes. |
| | | **Tests** — `tests:459-491`, `test_trusted_manifest_container_enum_outputs_one_json_object_and_exit_2`, subTests both `("expected_review_mode", [])` and `("expected_coverage_scope", [])` (`:460-463`) against the real CLI, asserting the same exact properties: exit 2 (`:483`), `stderr == ""` (`:484`), one stdout line (`:485`) parsed whole by `json.loads` (`:486`), `tool_input_failure` (`:488`), `trusted_manifest_invalid` (`:490`), `assertNotIn("schema_validation")` (`:491`). |
| **Crash-class audit (AC-R3-02 scope)** | **fixed** | Every remaining hash/membership site in the two trusted-input validators is type-gated before use: `:367` `finding_id in seen` and `:370` `seen.add` sit in the `elif`/`else` of `_is_finding_id(finding_id)` (`:365`, itself `isinstance`-guarded at `:282`); `:447` `criterion_id in criterion_ids` and `:449` `.add` are reached only past `isinstance(criterion.get("criterion_id"), str)` with `continue` (`:443-445`); `:361`/`:440` `set(item)`/`set(criterion)` operate on `dict` keys after `isinstance(..., dict)` checks (`:358`, `:438`); `:350-353` gates `findings` as a `list`; `:391` gates `subject` as a `dict`; `:429` gates `criteria` as a `list`; `_is_sha256_digest` (`:275-278`) is `isinstance`-guarded internally. Root-level non-objects are rejected upstream at `:159-160`. The remaining unguarded hashes (`:548`, `:551`, `:619`) consume **document** values, which reach `semantic_errors` only after JSON Schema validation early-returns at `:758-760` — pre-existing design, untouched by these guards, and the schema file is outside my inspect scope so I make no claim about it. |
| **No new authority** | **fixed** | Rework 3 adds only two `isinstance` guards and two test methods. No new error code: `prior_findings_invalid` and `trusted_manifest_invalid` both already existed in Rework 2 (EVIDENCE `:314-317`). The result shape is unchanged — `_failure_result:689-701` and the success dict `:766-772` emit only `mode`/`contract_valid`/`failure_kind`/`layers`/`errors`; the 0/1/2 exit contract at `:825-827` is intact. `tests:114-115` still asserts no `ACCEPTED`/`REWORK`/`FAILED_INFRA`/`ESCALATED` token appears in stdout. The guards can only make a path fail closed — they cannot accept, resolve, approve, or downgrade a severity; a container-valued status now yields exit 2 instead of a traceback, and no finding severity is touched. EVIDENCE `:455-457` still states the slice does not accept, merge, resolve, approve, classify `FAILED_INFRA`, or mutate state. |

## Rework-introduced blocker/major caused by these guards

None. The guards are pure short-circuit additions: for `str` values both operands evaluate exactly as before (so the valid fixtures and the 20 preserved tests are unaffected by construction), and for non-`str` values they replace an uncaught `TypeError` with the error branch that hashable non-strings already took.

## Consistency notes on EVIDENCE.md (not findings)

EVIDENCE `:100-105` claims 22 tests pass; the test file does contain exactly 22 `test_` methods (20 prior + the 2 new regressions), so the count is internally consistent. The `:74-78` regression transcript matches the codes the source produces on those three inputs. Both are corroborating, not proof — I could not run them.

`TARGETED_PASS`
