Bash execution was denied in this session (only read-only file listing/grep succeeded), so everything below is **source-verified static analysis**. EVIDENCE.md's run outputs (20 tests OK, `py_compile`, fixture sweep) are self-asserted claims I could not reproduce — though case_1/case_2/case_3 outputs in EVIDENCE.md:129-134 match exactly what the source produces on those inputs, which is corroborating but not proof.

# Final targeted review: RV-11 · RI-01 · RV-03

| ID | Status | Evidence | Remaining failure scenario |
|---|---|---|---|
| RV-11 remainder | **fixed** | `validate_review_verdict.py:385-466` now gates every binding before use: `document_type` (`:385`), `subject` must be an object with *exactly* `REQUIRED_MANIFEST_SUBJECT_KEYS` (`:391-399`, list at `:23-34`), both input digests must be real sha256 (`:405-411`), `expected_review_mode`/`expected_coverage_scope` must be enum members (`:415-424`), `criteria` must be a non-empty array of unique `{criterion_id, statement_digest}` records (`:428-453`). All emit `_tool_error` → `failure_kind: tool_input_failure` (`:764`) → exit 2 (`:827`). No binding is skipped silently: every previously `get`-with-default path now emits an error first, and even where a check is then bypassed (e.g. `:463` and `:626` no-op when `criterion_ids` is empty), the run has already failed. Regression: `tests:379-386` feeds the exact `{"document_type":"trusted_review_manifest"}` reproduction and requires `trusted_manifest_invalid` + `tool_input_failure` + `assertNotIn("schema_validation")`. | Manifest `schema_version` and unknown top-level manifest keys are unvalidated (prior-findings *is* version-checked at `:347`) — asymmetric, but no binding depends on them and key typos still surface as a missing required field. Separately: the new `:415-424` checks are the crash site in RI-02 below. |
| RI-01 | **partially_fixed** | The named reproduction is closed: `_validate_prior_findings_structure:350-353` type-guards `findings` before iteration and returns `False`, so `_prior_open_ids:335-340` (the old `TypeError` site) is unreachable on malformed input, and the coverage comparison is gated on `prior_findings_valid` (`:640`). `tests:388-415` runs the exact `{"document_type":"prior_findings","findings":5}` case through the real CLI and requires exit 2, `stderr == ""`, **one** stdout line, `prior_findings_invalid`, and no `schema_validation`. | **The crash class is not closed at every branch.** `:372` does `if status not in PRIOR_FINDING_STATUSES` (a `set`, `:37`) on an unvalidated JSON value. `--prior-findings` = `{"document_type":"prior_findings","schema_version":"1.0.0","findings":[{"finding_id":"fnd_<32 hex>","status":[]}]}` → `TypeError: unhashable type: 'list'` at `:372`, propagating through `semantic_errors` → `validate_document` → `main` (no handler anywhere, `:797-831`) → traceback on stderr, **empty stdout, exit 1**. Same shape RI-01 was filed for. Fix: `if not isinstance(status, str) or status not in ...`. |
| RV-03 remainder | **fixed** | Expected mode and scope are now externally bound and compared: `:415-420` (`review_mode` vs `expected_review_mode`) and `:422-426` (`coverage_scope` vs `expected_coverage_scope`), sourced only from `--trusted-manifest` (`:802`, loaded through the same strict transport at `:737`) and unexpressible in the document (schema root `additionalProperties: false`). The bypass is closed twice over: `requires_prior` at `:476-480` now fires on `expected_review_mode == "targeted_verification"` **or** `isinstance(document.get("verification"), dict)`, independent of the reviewer-chosen `review_mode`. `tests:417-425` applies the exact mutation (`review_mode → final_full`, `coverage_scope → full` on the valid targeted fixture) and requires both `prior_findings_missing` and `trusted_binding_mismatch`, with `assertNotIn("schema_validation")` — correct, since schema `:532-557` genuinely permits `final_full` + `verification`. The opposite direction is preserved: `tests:427-438` validates a legitimate `final_full` + `verification` document against a `final_full/full` manifest with real prior data and asserts `errors == []`; source-consistent, since the targeted fixture's coverage is exactly `{AC-1}` (fixture `:100`), matching the manifest's single criterion, so the full-mode exact-coverage branch at `:463-465` does not fire. | Residual of the same design, outside the named bypass: when the coordinator legitimately expects `final_full` and *omits* the verification block, `requires_prior` is `False` (`:476-480`), so a supplied `--prior-findings` file is **ignored entirely with no error** and `contract_valid: true` is returned. There is no coordinator-side "verification required" flag. If a final full review is meant to re-verify prior open findings, that is enforced by coordinator policy only, not by this validator. |

## Rework-introduced finding

**RI-02 — new trusted-manifest mode/scope checks crash on non-hashable JSON values (major).**
`:416` (`expected_review_mode not in REVIEW_MODES`) and `:423` (`expected_coverage_scope not in COVERAGE_SCOPES`) test membership in `set` literals (`:35-36`) against raw manifest values. `--trusted-manifest` = `{"document_type":"trusted_review_manifest","expected_review_mode":[]}` reaches `:416` (only `document_type` gates earlier, `:385`) and raises `TypeError: unhashable type: 'list'` → traceback, empty stdout, exit 1. This code is new in Rework 2 and violates the contract README `:66-68` states ("Every CLI path prints one JSON object") and AC-R2-02. Same one-line fix shape as RI-01's remainder: guard with `isinstance(..., str)` before the membership test. Confidence high from source (JSON objects/arrays decode to `dict`/`list`, both unhashable; `set.__contains__` propagates `TypeError` for non-set keys) — **not executed**, Bash was denied.

## Boundary check

No new authority. The rework adds only input-validation codes (`trusted_manifest_invalid`, `prior_findings_invalid`) and comparison codes (`trusted_binding_mismatch`, `criteria_coverage_mismatch`, `missing_criteria_coverage`). Output remains `mode`/`contract_valid`/`failure_kind`/`layers`/`errors` (`:689-701`, `:766-772`); no acceptance, resolution, severity-downgrade, or approval field or code was introduced; the forbidden-field `not/anyOf` list is intact (schema `:290-346`); `tests:114-115` still asserts no decision token in stdout. The coordinator-supplied `expected_review_mode`/`expected_coverage_scope` are input bindings, not decisions.

`TARGETED_REWORK`
