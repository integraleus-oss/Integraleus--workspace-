## 2026-03-27

- 07:35 UTC heartbeat: Telegram liveness OK (`@Integraleus45bot`, ~75ms).
- Main host security check: `ufw` absent (`ufw not installed`), users unchanged (`root`, `ops`), sshd config files unchanged (`50-cloud-init.conf`, `60-cloudimg-settings.conf`, `99-hardening.conf`), no new suspicious login IPs in `last -5`.
- Garden check OK: known IPs only, `permitrootlogin without-password`, `passwordauthentication no`, UFW active, `openclaw-gateway`/`fail2ban`/`ssh` active, no OAuth/Codex refresh errors.
- Cron review: morning jobs completed successfully, but results are inconsistent:
  - `system-monitor:daily` correctly flagged missing firewall on main host.
  - `Daily Server Monitoring` incorrectly reported `Main: UFW ✅`.
- Cron usage snapshot for today so far: SU1531 flight check 36.5k tokens, BBQ Daily Digest 21.5k, Daily Server Monitoring 20.9k, system-monitor:daily 19.1k; total ~98.0k, no single run >50k, daily total still <200k.
- Worth alerting user: main host appears to be without active UFW while one cron is masking it with a false OK.
- 12:01 UTC heartbeat: Telegram OK (~70ms), Garden services/OAuth OK. `openclaw status --deep` shows a **CRITICAL Discord security warning**: `Discord guilds: groupPolicy="open" allows any channel not explicitly denied to trigger (mention-gated)`. Also a non-critical reverse proxy headers warning is present.
- Correction: the earlier "UFW missing on main" alert was false. Root cause: heartbeat/manual checks used bare `ufw` in a non-login shell where `PATH` lacked `/usr/sbin` and `/sbin`, so `ufw` resolved as "command not found" even though `/usr/sbin/ufw` exists and reports `Status: active`. Main UFW rules confirmed active for `22/tcp`, `80/tcp`, `443/tcp`; Garden UFW also active. Updated `HEARTBEAT.md` to require absolute paths for admin tools.
- 16:44 UTC: Full evening pass — all OK. Main UFW active (confirmed via `/usr/sbin/ufw`), Garden OK, Telegram OK, no new SSH logins, no OAuth errors.
- 18:14 UTC: Patched `system-monitor:daily` cron prompt to use `/usr/sbin/ufw status` and added explicit warning about absolute paths. Next run (tomorrow 10:30 MSK) should no longer produce false UFW alerts.
