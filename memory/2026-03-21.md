# 2026-03-21 (Saturday)

## Garden OAuth Fix + Fallback Setup

### Проблема
- Garden (`31.128.32.68`) — OAuth токен OpenAI Codex протух
- Логи: `OAuth token refresh failed for openai-codex`
- Все запросы к gpt-5.4 фейлились

### Решение
1. Пользователь зашёл по SSH на Garden, запустил `openclaw configure --section model`
2. Прошёл OAuth re-auth через браузер
3. Токен обновлён, gateway перезапущен — работает

### Fallback chain — Garden
- Primary: `openai-codex/gpt-5.4` (OAuth)
- Fallback: `openrouter/qwen/qwen3-coder-480b:free` (бесплатная)
- OpenRouter API key: `openrouter:garden` profile (отдельный ключ для Garden)

### Fallback chain — Main (155.212.227.115)
- Primary: `anthropic/claude-opus-4-6` (API key)
- Fallback 1: `openai-codex/gpt-5.4` (OAuth)
- Fallback 2: `openrouter/qwen/qwen3-coder-480b:free` (бесплатная)
- OpenRouter API key: `openrouter:fallback` profile (тот же ключ что на Garden)

### OAuth token status (Main)
- `openai-codex:default`: access token expires ~31 марта 2026
- Refresh token должен обновлять автоматически (~90 дней lifetime)
- Anthropic: API key, не протухает
- OpenAI: API key, не протухает

### Мониторинг
- Добавил в HEARTBEAT.md проверку Garden логов на OAuth ошибки
- Команда: `journalctl -u openclaw-gateway --since '2 hours ago' | grep -iE 'oauth|refresh.*fail|token.*fail|auth.*error'`
- При обнаружении — алерт пользователю с инструкцией `openclaw configure --section model`

### Обновления
- Main: обновлён до 2026.3.13 (538 пакетов)
- Garden: обновлён до 2026.3.13 (538 пакетов)

### Как работает fallback
- Gateway автоматически переключает при ошибке (401, 500, rate limit, таймаут)
- Переключение прозрачное, задержка 1-3 секунды
- Не требует действий от агента
