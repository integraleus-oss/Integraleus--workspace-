# 2026-04-03

## 06:03 MSK — Ollama зависла, высокая нагрузка CPU

**Проблема:** Load average 4.09 (норма ~0.15)
**Причина:** `ollama runner` процесс с моделью `nomic-embed-text` зажрал 391% CPU
**Детали:** Модель должна была выгрузиться в 21:08 (expires_at), но процесс завис
**Решение:** Остановил ollama, load упала до 3.20 и продолжает снижаться

## 08:01 MSK — Разбор и исправление

**Корень проблемы:** Скрипты `build_index_batch.py` и `rebuild_index.py` бомбили ollama сотнями embedding-запросов подряд без пауз.

**Пример:** Файл `AlphaPlatform_1018-2000_text.md` (1MB) → 918 чанков → 918 подряд POST /api/embed, каждый по 4-6 секунд.

**Исправления:** Добавлены `time.sleep(0.1)` в:
- `build_index_batch.py` — после каждого embedding
- `rebuild_index.py` — после каждого embedding  
- `rag.py` — добавлен `import time` + `time.sleep(0.05)` в batch embedding

**Тестирование:** 
- Ollama перезапущена, стабильна (load 0.02)
- 3 тестовых embedding — нормально (0.47s, 0.04s, 0.04s)
- CPU runner'а: 391% → 2.7% (стабильно)
- Alpha-Bot перезапущен с исправлениями

**Результат:** Проблема устранена. Rate limiting предотвратит зависания при массовой индексации.

## 08:38 MSK — Обновление OpenClaw + проблемы

### Хронология инцидента
1. Обновление 2026.4.1 → 2026.4.2 прошло, но gateway остался с битыми runtime-модулями
2. Ошибки: `ERR_MODULE_NOT_FOUND` (store.runtime, audit-membership-runtime)
3. Telegram получил polling conflict (409) — два инстанса после рестарта
4. Opus таймаутился, fallback на Codex срабатывал
5. Пользователь получил "⚠️ Something went wrong..."

### Исправление
- `openclaw doctor --fix` — починил missing modules
- После fix ошибки пропали

### ЛЕКЦИИ (записать навсегда)
1. **После `openclaw update` ОБЯЗАТЕЛЬНО:** doctor --fix → проверить логи → restart gateway → status --deep
2. **Не использовать `kill -TERM`** для gateway — только `openclaw gateway restart`
3. **Не использовать `systemctl restart openclaw-gateway`** (без --user) — нет такого сервиса
4. **Меньше спрашивать, больше делать** — если задача понятна, не предлагать варианты, а делать

## Самооценка дня: 6/10
- Диагностика: 8/10 (отлично)
- Исполнение: 5/10 (плохо, много проб методом тыка)
- Знание инструментов: 4/10 (не знал команды gateway)
- Коммуникация: 6/10 (много воды)

## Остальной мониторинг
- Telegram: OK (73ms)
- Garden: OK, все сервисы активны  
- Main: disk 83% → 82% (почистили ~2ГБ)
- OpenClaw: 2026.4.2 (up to date)
- Ollama: починена, rate limiting добавлен

## 16:17 MSK — Ollama снова зависла (383% CPU)
**Причина:** `rebuild_index.py` запущен вручную из tmux в 11:41 — до моего фикса rate limiting.
Скрипт работал со старым кодом в памяти (без sleep).
**Решение:** Убил процесс, перезапустил ollama. Load: 4.27 → 2.85 и падает.
**Важно:** Нужно сообщить Станиславу — если он запускает rebuild_index.py, надо делать это из обновлённого кода.
# 2026-04-03

## 06:03 MSK — Ollama зависла, высокая нагрузка CPU

**Проблема:** Load average 4.09 (норма ~0.15)
**Причина:** `ollama runner` процесс с моделью `nomic-embed-text` зажрал 391% CPU
**Детали:** Модель должна была выгрузиться в 21:08 (expires_at), но процесс завис
**Решение:** Остановил ollama, load упала до 3.20 и продолжает снижаться

## 08:01 MSK — Разбор и исправление

**Корень проблемы:** Скрипты `build_index_batch.py` и `rebuild_index.py` бомбили ollama сотнями embedding-запросов подряд без пауз.

**Пример:** Файл `AlphaPlatform_1018-2000_text.md` (1MB) → 918 чанков → 918 подряд POST /api/embed, каждый по 4-6 секунд.

**Исправления:** Добавлены `time.sleep(0.1)` в:
- `build_index_batch.py` — после каждого embedding
- `rebuild_index.py` — после каждого embedding  
- `rag.py` — добавлен `import time` + `time.sleep(0.05)` в batch embedding

**Тестирование:** 
- Ollama перезапущена, стабильна (load 0.02)
- 3 тестовых embedding — нормально (0.47s, 0.04s, 0.04s)
- CPU runner'а: 391% → 2.7% (стабильно)
- Alpha-Bot перезапущен с исправлениями

**Результат:** Проблема устранена. Rate limiting предотвратит зависания при массовой индексации.

## 08:38 MSK — Обновление OpenClaw + проблемы

### Хронология инцидента
1. Обновление 2026.4.1 → 2026.4.2 прошло, но gateway остался с битыми runtime-модулями
2. Ошибки: `ERR_MODULE_NOT_FOUND` (store.runtime, audit-membership-runtime)
3. Telegram получил polling conflict (409) — два инстанса после рестарта
4. Opus таймаутился, fallback на Codex срабатывал
5. Пользователь получил "⚠️ Something went wrong..."

### Исправление
- `openclaw doctor --fix` — починил missing modules
- После fix ошибки пропали

### ЛЕКЦИИ (записать навсегда)
1. **После `openclaw update` ОБЯЗАТЕЛЬНО:** doctor --fix → проверить логи → restart gateway → status --deep
2. **Не использовать `kill -TERM`** для gateway — только `openclaw gateway restart`
3. **Не использовать `systemctl restart openclaw-gateway`** (без --user) — нет такого сервиса
4. **Меньше спрашивать, больше делать** — если задача понятна, не предлагать варианты, а делать

## Самооценка дня: 6/10
- Диагностика: 8/10 (отлично)
- Исполнение: 5/10 (плохо, много проб методом тыка)
- Знание инструментов: 4/10 (не знал команды gateway)
- Коммуникация: 6/10 (много воды)

## Остальной мониторинг
- Telegram: OK (73ms)
- Garden: OK, все сервисы активны  
- Main: disk 83% → 82% (почистили ~2ГБ)
- OpenClaw: 2026.4.2 (up to date)
- Ollama: починена, rate limiting добавлен

## 16:17 MSK — Ollama снова зависла (383% CPU)
**Причина:** `rebuild_index.py` запущен вручную из tmux в 11:41 — до моего фикса rate limiting.
Скрипт работал со старым кодом в памяти (без sleep).
**Решение:** Убил процесс, перезапустил ollama. Load: 4.27 → 2.85 и падает.
**Важно:** Нужно сообщить Станиславу — если он запускает rebuild_index.py, надо делать это из обновлённого кода.

## 09:03 MSK — Переключения моделей
- Sonnet → Codex (GPT-5.4) по просьбе Станислава
- Codex → Opus по просьбе Станислава
- Opus таймаутился → fallback на Codex работал
- В итоге остались на Opus, стабилизировалось

## 09:05 MSK — Аудит OpenClaw
- OpenClaw 2026.4.2, gateway reachable ~25ms
- Telegram OK ~79ms, Discord OK ~541ms
- 4 CRITICAL security warnings в audit (Discord groupPolicy=open, exec security=full)
- Предложен план: установить gateway как systemd-сервис, закрыть Discord security, настроить fallback timeout

## 10:05 MSK — VPN обновлён и перезагружен
- 29 пакетов обновлено на VPN (157.22.180.83)
- Ядро 6.8.0-106 → 107
- UFW был неактивен после ребута! Включил и поставил на автозапуск (`ufw enable` + `systemctl enable ufw`)
- **УРОК:** После ребута VPN проверять UFW — он может не стартовать автоматически

## 14:08 MSK — Cursor обсуждение
- Станислав прислал cursor.com
- Мой вердикт: для headless VPS не нужен, для GEEKOM с монитором — да
- Для фронтенда (specialtechnology.ru) — Cursor лучше чем headless Claude Code
- Решение: подождём GEEKOM

## 14:16 MSK — Перелинковка блога specialtechnology.ru
- Запустил субагента
- **Результат за 7 минут:**
  - Удалено 13 битых ссылок (import-substitution, oil-refinery, scada-comparison)
  - Добавлено 79 новых контекстных ссылок в 20 статьях
  - Каждая статья: 6-10 уникальных перекрёстных ссылок
  - 0 битых ссылок осталось
- Задеплоено на сервер, закоммичено в git

## 20:04 MSK — Очистка диска
- npm cache: 919 МБ
- apt cache: 131 МБ
- Старое ядро 6.8.0-106: ~300 МБ
- Журналы: 52 МБ
- **Итого: 83% → 80%** (7.9 ГБ свободно)
- Оставшиеся кандидаты: snap (2.2 ГБ), whisper venv (530 МБ)

## Известный IP добавлен
- 31.10.95.23 — Станислав (подтвердил). Добавлен в USER.md и heartbeat-state.json.
# 2026-04-03

## 06:03 MSK — Ollama зависла, высокая нагрузка CPU

**Проблема:** Load average 4.09 (норма ~0.15)
**Причина:** `ollama runner` процесс с моделью `nomic-embed-text` зажрал 391% CPU
**Детали:** Модель должна была выгрузиться в 21:08 (expires_at), но процесс завис
**Решение:** Остановил ollama, load упала до 3.20 и продолжает снижаться

## 08:01 MSK — Разбор и исправление

**Корень проблемы:** Скрипты `build_index_batch.py` и `rebuild_index.py` бомбили ollama сотнями embedding-запросов подряд без пауз.

**Пример:** Файл `AlphaPlatform_1018-2000_text.md` (1MB) → 918 чанков → 918 подряд POST /api/embed, каждый по 4-6 секунд.

**Исправления:** Добавлены `time.sleep(0.1)` в:
- `build_index_batch.py` — после каждого embedding
- `rebuild_index.py` — после каждого embedding  
- `rag.py` — добавлен `import time` + `time.sleep(0.05)` в batch embedding

**Тестирование:** 
- Ollama перезапущена, стабильна (load 0.02)
- 3 тестовых embedding — нормально (0.47s, 0.04s, 0.04s)
- CPU runner'а: 391% → 2.7% (стабильно)
- Alpha-Bot перезапущен с исправлениями

**Результат:** Проблема устранена. Rate limiting предотвратит зависания при массовой индексации.

## 08:38 MSK — Обновление OpenClaw + проблемы

### Хронология инцидента
1. Обновление 2026.4.1 → 2026.4.2 прошло, но gateway остался с битыми runtime-модулями
2. Ошибки: `ERR_MODULE_NOT_FOUND` (store.runtime, audit-membership-runtime)
3. Telegram получил polling conflict (409) — два инстанса после рестарта
4. Opus таймаутился, fallback на Codex срабатывал
5. Пользователь получил "⚠️ Something went wrong..."

### Исправление
- `openclaw doctor --fix` — починил missing modules
- После fix ошибки пропали

### ЛЕКЦИИ (записать навсегда)
1. **После `openclaw update` ОБЯЗАТЕЛЬНО:** doctor --fix → проверить логи → restart gateway → status --deep
2. **Не использовать `kill -TERM`** для gateway — только `openclaw gateway restart`
3. **Не использовать `systemctl restart openclaw-gateway`** (без --user) — нет такого сервиса
4. **Меньше спрашивать, больше делать** — если задача понятна, не предлагать варианты, а делать

## Самооценка дня: 6/10
- Диагностика: 8/10 (отлично)
- Исполнение: 5/10 (плохо, много проб методом тыка)
- Знание инструментов: 4/10 (не знал команды gateway)
- Коммуникация: 6/10 (много воды)

## Остальной мониторинг
- Telegram: OK (73ms)
- Garden: OK, все сервисы активны  
- Main: disk 83% → 82% (почистили ~2ГБ)
- OpenClaw: 2026.4.2 (up to date)
- Ollama: починена, rate limiting добавлен

## 16:17 MSK — Ollama снова зависла (383% CPU)
**Причина:** `rebuild_index.py` запущен вручную из tmux в 11:41 — до моего фикса rate limiting.
Скрипт работал со старым кодом в памяти (без sleep).
**Решение:** Убил процесс, перезапустил ollama. Load: 4.27 → 2.85 и падает.
**Важно:** Нужно сообщить Станиславу — если он запускает rebuild_index.py, надо делать это из обновлённого кода.

## 09:03 MSK — Переключения моделей
- Sonnet → Codex (GPT-5.4) по просьбе Станислава
- Codex → Opus по просьбе Станислава
- Opus таймаутился → fallback на Codex работал
- В итоге остались на Opus, стабилизировалось

## 09:05 MSK — Аудит OpenClaw
- OpenClaw 2026.4.2, gateway reachable ~25ms
- Telegram OK ~79ms, Discord OK ~541ms
- 4 CRITICAL security warnings в audit (Discord groupPolicy=open, exec security=full)
- Предложен план: установить gateway как systemd-сервис, закрыть Discord security, настроить fallback timeout

## 10:05 MSK — VPN обновлён и перезагружен
- 29 пакетов обновлено на VPN (157.22.180.83)
- Ядро 6.8.0-106 → 107
- UFW был неактивен после ребута! Включил и поставил на автозапуск (`ufw enable` + `systemctl enable ufw`)
- **УРОК:** После ребута VPN проверять UFW — он может не стартовать автоматически

## 14:08 MSK — Cursor обсуждение
- Станислав прислал cursor.com
- Мой вердикт: для headless VPS не нужен, для GEEKOM с монитором — да
- Для фронтенда (specialtechnology.ru) — Cursor лучше чем headless Claude Code
- Решение: подождём GEEKOM

## 14:16 MSK — Перелинковка блога specialtechnology.ru
- Запустил субагента
- **Результат за 7 минут:**
  - Удалено 13 битых ссылок (import-substitution, oil-refinery, scada-comparison)
  - Добавлено 79 новых контекстных ссылок в 20 статьях
  - Каждая статья: 6-10 уникальных перекрёстных ссылок
  - 0 битых ссылок осталось
- Задеплоено на сервер, закоммичено в git

## 20:04 MSK — Очистка диска
- npm cache: 919 МБ
- apt cache: 131 МБ
- Старое ядро 6.8.0-106: ~300 МБ
- Журналы: 52 МБ
- **Итого: 83% → 80%** (7.9 ГБ свободно)
- Оставшиеся кандидаты: snap (2.2 ГБ), whisper venv (530 МБ)

## Известный IP добавлен
- 31.10.95.23 — Станислав (подтвердил). Добавлен в USER.md и heartbeat-state.json.

## 20:50-23:30 MSK — Переезд на новый ПК (openclaw-home)

### Контекст
- Новый ПК: Ubuntu 24.04, 16 ядер, 12GB RAM, 937GB NVMe (GEEKOM)
- Цель: новый ПК станет основным (вместо VPS), но сначала тестовый период
- Доступ через reverse SSH tunnel: `ssh -N -R 2222:localhost:22 root@155.212.227.115`
- Пользователь подключается к ПК через Termius

### Что сделано
1. **Telegram отключён на новом ПК** — убрал конфликт 409 с VPS
2. **OpenClaw починен** — битая установка после неудачного обновления, переустановлен до 2026.4.2
3. **Диагностика сети нового ПК** — обнаружена критическая проблема

### Проблема с сетью нового ПК
- Пинг нормальный (42ms до 8.8.8.8, 0% потерь)
- HTTP/HTTPS скачивание ~258 байт/сек (!!!)
- DNS резолвится нормально
- TCP до 443 устанавливается, затык на TLS handshake после Client Hello
- VPN (AmneziaWG) НЕ причина — default route через локальный роутер 192.168.68.1
- **Гипотеза: MTU / PMTU blackhole** — запросил у Станислава проверку `ping -M do -s 1472`

### Что сделано (финал, ~00:00 MSK)
- **Ollama 0.20.0 + nomic-embed-text** установлены и работают (embedding за 40ms!)
- **Gateway запущен** через systemd, enabled, reachable 30ms
- **Discord подключён** и работает через AmneziaWG VPN (200 OK, 1324ms)
- **Memory search** работает: 21 файл, 101 чанк, vector ready
- **VPN (awg0)** перезапущен и работает (full tunnel через 157.22.180.83)
- **MTU eno1** возвращён на 1500 (записан persistent .link файл)
- Все сервисы на autostart: openclaw-gateway, ollama, awg-quick@awg0
- **Скорость интернета:** без VPN ~50 MB/s, через VPN ~54 KB/s (ожидаемо для full tunnel)

### Диагностика сети (решено)
- Проблема: медленное скачивание (~258 B/s) при нормальном пинге
- Причина: НЕ MTU blackhole (как думали), а **VPN awg0 full tunnel** — весь трафик шёл через медленный VPN
- awg0 down → скорость всё ещё 3 KB/s → дело было не только в VPN
- Реальная причина: CDN некоторых сайтов (ubuntu, ollama CDN) тормозили, GitHub и kernel.org работали на 50+ MB/s
- Решение: установка Ollama пошла через скрипт, который качает с GitHub — 53 MB/sec

### Что НЕ сделано
- Telegram на новом ПК **отключён** — включить при переводе с VPS
- Security audit: 5 CRITICAL (Discord open policy) — как и на VPS
- Workspace не синхронизирован с VPS (нужно при полном переезде)

## 08:05-08:37 MSK (04.04) — Tailscale + Synology NAS

### Tailscale настроен
- Tailscale установлен на VPS (`openclaw-vps`, 100.127.146.46)
- Новый ПК уже был в Tailscale (`openclaw-home`, 100.114.189.16)
- SSH через Tailscale работает: `ssh stanislav@openclaw-home`
- **Больше не нужны ручные reverse tunnel!**
- Ping 41ms между VPS и новым ПК

### Synology NAS подключён
- IP: 192.168.68.103, DSM на :5000/:5001
- NFS share: `/volume1/Documents` → `/mnt/synology/Documents` на openclaw-home
- Объём: 7.3 TB (3.6 TB занято)
- Squash: Map all users to admin (доступ без sudo)
- fstab прописан, автомонтирование при ребуте
- Папки: OneDrive, Книги, Журналы, Загрузки, Для сайтов, Флешка_SE, nata
- SSH на Synology пока отключён — включим позже

### Правило безопасности (D-2026-04-04-01)
- **Данные Synology НЕ покидают домашнюю сеть** без явного разрешения Станислава
- Записано в DECISIONS.md и TOOLS.md

### Домашняя сеть (192.168.68.0/24)
- Роутер: 192.168.68.1
- Новый ПК: 192.168.68.125
- Synology: 192.168.68.103
- Всего 17 устройств в сети

### Туннель
- Туннель нестабильный — падал несколько раз за сессию
- Порт 2222 зависал от старых процессов, приходилось чистить
- **УРОК:** npm install и длинные операции через туннель надо запускать через `nohup` в фоне

### Ключевые уроки переезда
1. **Reverse SSH tunnel ненадёжен** для длинных операций — использовать nohup
2. **Не пытаться качать через туннель** — если на ПК есть интернет, пусть качает сам
3. **Сначала диагностировать сеть** — до начала установок проверить скорость скачивания
4. **Не лезть на Garden** для задач нового ПК — разные агенты, разные серверы