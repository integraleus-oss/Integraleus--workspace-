## 14:19 UTC — Remote OpenClaw agent recovery on VPS (46.173.17.111)
- User requested fully hands-off fix via SSH (`root` on Ubuntu 24.04).
- Diagnosed remote issue: OpenClaw gateway was not supervised and had service/startup conflicts.
- Implemented and enabled systemd unit `/etc/systemd/system/openclaw-gateway.service`, cleaned conflicting manual `nohup` gateway process, and verified gateway listener on `127.0.0.1:18789`.
- Confirmed model target remains `openai/gpt-5.3-codex` (Codex 5.3).
- Root cause for model failures evolved:
  1) invalid/poisoned auth token profile (`Symbol(clack:cancel)`), causing 401;
  2) after auth file reset and gateway restart, 401 resolved but account returns API rate limit.
- Durable fix applied in remote auth file: rewrote `/root/.openclaw/agents/main/agent/auth-profiles.json` to only `openai:default` API key profile and removed poisoned manual token profile.
- Final functional state: no more 401 invalid-key error; remaining blocker is OpenAI rate-limit/quota on provided key.
- Security note communicated: credentials were shared in chat; advised rotating root password/API keys.
