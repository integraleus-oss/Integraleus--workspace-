# 2026-03-10

## Cron jobs — schedule spread to 30-min gaps
- security-audit: Mon 9:00
- update-status: Mon 9:30
- BBQ Daily Digest: daily 10:00
- system-monitor: daily 10:30 & 18:30
- All set to Claude Opus with 300s timeout

## Cron failover mechanism added to HEARTBEAT.md
- If cron job has `lastRunStatus: "error"` + "timed out" → auto-switch to `openai-codex/gpt-5.4`, re-run, alert user
- Do NOT auto-switch back to Claude — user decides
- Resource tracking also added: token usage, duration, alerts if >50k tokens/run or >200k/day

## Claude Opus cron hang — root cause analysis
Two more hangs observed:
1. system-monitor:daily — started 04:20 UTC, timed out after 3600s (lane=cron, queueAhead=0)
2. system-monitor (again) — started 07:55 UTC, same pattern

Key findings from logs:
- `lane wait exceeded: lane=cron waitedMs=3599848 queueAhead=0` — nothing ahead in queue
- `Profile anthropic:default timed out. Trying next account...`
- Heartbeat session (04:55 UTC) with Claude Opus WORKS (34s, same model, same API key)
- Direct chat with Claude Opus WORKS fine

**Difference**: heartbeat/direct = existing sessions with warm cache. Cron = new isolated sessions, cold start, no cache.

**Most likely cause**: Claude Opus cold-start without prompt cache is too slow or unstable for cron lane. Codex handles same tasks in 52s.

**Decision**: cron stays on Codex, direct chat on Claude Opus. This is the stable architecture.

## ТЗ SCADA O-PAS v7 received
- File: `TZ_SCADA_OPAS_v7.docx` (saved to workspace as `TZ_SCADA_OPAS_v7.docx` + extracted `TZ_SCADA_OPAS_v7.txt`)
- 784 lines, 17 sections, 105 requirements in matrix (REQ-001 to REQ-105)
- Full SCADA spec for O-PAS v2.1 open architecture
- Covers: visualization (14 screen types), dispatch control, alarms (ISA-18.2/EEMUA 191), journals (8 types), trends, configuration, reporting, diagnostics, roles (8), backup, security (IEC 62443 SL2), performance (≤2s SLA)
- Forwarded from user's own bot — this is user's own work
- User asked what to do with it — waiting for direction (review, comparison with v6, format conversion, etc.)

## BBQ Daily Digest — still failing
- Timed out on Claude (300s), switched to Codex via failover
- Job ID: 5947d8b3, delivers to group `-1003781262788` topic 873

## Two-day monitoring period started
- Watching cron stability for 2 days (until Thu evening)
- Will deliver summary report to user
