# 2026-03-31

- HumanLike Agent repeatedly lost Telegram authorization with `SessionRevokedError`; pattern suggests Telegram session invalidation / all sessions revoked. Re-authorization was performed multiple times, and `STRING_SESSION` in `projects/humanlike-agent/.env` was refreshed after successful auth.
- Important operational lesson: do not use broad kill commands like `killall python3` on the VPS. It killed unrelated Python services and contributed to HumanLike Agent instability. Prefer targeted PID/service restarts only.
- HumanLike Agent behavior was tuned several times:
  - mention detection improved to catch `tg://user?id=...` and entity-based mentions;
  - skip decisions now log as INFO for debugging;
  - response style pushed toward short chat-like answers by default, with longer answers only when explicitly asked;
  - message splitting was adjusted to avoid multi-part spammy output.
- HumanLike Agent hallucinated Alpha Platform internals (`alpha.script`, JS classes/inheritance claims). Guardrails were tightened:
  - persona now explicitly forbids inventing Alpha APIs/classes/methods/components;
  - when Alpha-related query has no RAG context, the agent must say it is unsure and refer to documentation instead of improvising;
  - outdated Alpha module mentions in persona were cleaned up.
- HumanLike RAG client had an indexing mismatch issue (`chunks.json` and `embeddings.npy` lengths diverged), causing `list index out of range`. A defensive fix was added in `projects/humanlike-agent/rag_client.py` to cap iteration at `min(len(_chunks), len(_embeddings))`.
- Rebuilding the Alpha-bot index in the background overloaded local inference / Ollama and caused HumanLike responses to stall badly. Operational rule: heavy index rebuilds should be scheduled off-hours, not while the chat agent is actively serving conversations.
- Alpha-bot licensing calculator work was completed earlier in this cycle: code-based calculation was introduced so tag math/recommendations are done by deterministic code instead of relying purely on LLM behavior.
- 10:15 MSK heartbeat: Telegram probe on main server is OK; no gateway restart needed. However, two 10:00 MSK cron jobs (`BBQ Daily Digest`, `Daily Server Monitoring`) failed again on delivery with `Error: Outbound not configured for channel: telegram`, even though both jobs generated otherwise-valid summaries. No timeout was involved, so auto-failover was not triggered.
